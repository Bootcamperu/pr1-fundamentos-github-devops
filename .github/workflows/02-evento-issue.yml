name: 02-Evento Issue - Ejecutando Trigger Interno -502
# Trigger
on:
  push:
    paths-ignore:
      - '*.md'
  issues:
    types:
    - opened
#Job
jobs:
  hola-mundo:
    # Runner
    runs-on: ubuntu-latest
    # Steps 
    steps:
    # Runs a single command using the runners shell
    - name: Hola Mundo
      run: echo "Hola Mundo Ejecucion desde Issue"
